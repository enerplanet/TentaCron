version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - revive
    - gosec
    - misspell
    - gocritic
    - funlen
  settings:
    funlen:
      # Production functions stay short and single-purpose; table-driven
      # tests are exempt below because their length is data, not logic.
      lines: 50
      statements: 40
      ignore-comments: true
    errcheck:
      exclude-functions:
        - (io.Closer).Close
        - (*database/sql.Rows).Close
        - (*os.File).Close
        # Writes to the CLI's stdout/stderr: a failed write there has no
        # better handling than the exit code the command already returns.
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
    revive:
      rules:
        - name: package-comments
          disabled: true
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - funlen

# Formatting is part of the gate: `golangci-lint run` reports unformatted
# files, `golangci-lint fmt` fixes them. Imports group standard library,
# third-party and this module, in that order.
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/enerplanet/tentacron
